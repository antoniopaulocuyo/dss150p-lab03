### Staging Execution Results

Executed `build_staging()` against a fresh extraction run:

| Dataset | Input rows | Staged rows | Rows removed |
|---|---|---|---|
| customers | 3,003 | 3,000 | 3 (duplicate business keys, collapsed via dedup) |
| products | 601 | 599 | 2 (1 duplicate collapsed via dedup, 1 quarantined) |
| orders | 50,005 | 49,998 | 7 (5 duplicates collapsed via dedup, 2 quarantined) |

**Quarantined records: 3**

| source_table | business_key | reason |
|---|---|---|
| products | P0078 | invalid_or_nonpositive_price |
| orders | O0004445 | invalid_status |
| orders | O0000112 | invalid_quantity |

This confirms:
- Deduplication and quarantine logic together account for every row removed between raw input and staged output — no rows were silently dropped.
- The negative-price product (`P0078`) and the two structurally invalid orders (`O0004445` status, `O0000112` quantity) were traceably quarantined with explicit reasons rather than discarded.
- The 2 orphan foreign-key records identified during profiling (1 orphan `customer_id`, 1 orphan `product_id`) were intentionally *not* quarantined at this stage — they remain in staged orders and will be caught during the curated-layer join in Task C, consistent with the spec's layering rules.